{
  "name": "example-hello-world",
  "dependencies": {
    "@nuxt/http": "^0.5.7",
    "@nuxtjs/vuetify": "^1.12.3",
    "nuxt-composition-api": "^0.10.4",
    "vue-i18n": "^8.18.2"
  },
  "devDependencies": {
    "nuxt": "^3.0.0"
  },
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "export": "nuxt build && nuxt export",
    "serve": "nuxt serve",
    "now-build": "NOW_BUILD=true nuxt build && NOW_BUILD=true nuxt export",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "post-update": "yarn upgrade --latest"
  }
}
